# Rubric — How Does the NestJS CQRS Heroes Module Actually Work?

## intent_category
Code Onboarding & Comprehension

## difficulty
Hard

---

## Rubric Evaluation

| # | ID | Section | Description | Weight | Rationale | Dependent On |
|---|---|---|---|---|---|---|
| 1 | reasoning-1 | Reasoning | States that @CommandHandler registers the handler class as the designated processor for a specific command type using NestJS metadata, and that CommandBus reads this metadata at registration time to build its internal routing map | critical | This helps developers understand how a plain command class gets connected to its handler, which is the starting point of the entire dispatch flow. | |
| 2 | reasoning-2 | Reasoning | Explains that mergeObjectContext() patches the publish() and publishAll() methods on the specific hero instance to route through the EventBus, replacing the no-op stubs that AggregateRoot provides by default | critical | This helps developers avoid the common assumption that AggregateRoot automatically routes events to the EventBus, when in fact the patching step is what makes event delivery possible. | 1 |
| 3 | reasoning-3 | Reasoning | Explains that apply() stages the event in AggregateRoot's internal event list without dispatching it | critical | This helps developers understand why events do not appear at handlers immediately when apply() is called, preventing confusion about silent event failures. | 2 |
| 4 | reasoning-4 | Reasoning | Explains that commit() flushes the staged events by calling publishAll(), which routes each event through the EventBus | critical | This helps developers understand what actually triggers event delivery and why commit() must be called explicitly after every domain operation. | 3 |
| 5 | reasoning-5 | Reasoning | Explains that CqrsModule subscribes to the saga's returned Observable of ICommand at bootstrap and automatically dispatches every emitted command through the CommandBus | critical | This helps developers understand the saga contract correctly and avoid writing saga code that manually calls dispatch, which would be structurally wrong. | 4 |
| 6 | reasoning-6 | Reasoning | Explains that @Saga() is discovered by CqrsModule at bootstrap, which calls dragonKilled with the global EventBus observable stream as events$ | critical | This helps developers understand where events$ comes from and why the saga receives events without requiring any manual subscription setup. | |
| 7 | reasoning-7 | Reasoning | Explains that CqrsModule.forRoot() registers the module as globally scoped, making its exported providers available to all feature modules without each one importing CqrsModule | critical | This helps developers avoid the common NestJS mistake of assuming every feature module must import CqrsModule individually to access its providers. | |
| 8 | reasoning-8 | Reasoning | Explains that Scope.REQUEST causes NestJS to create a new handler instance per incoming request rather than reusing a singleton | critical | This helps developers understand the lifecycle difference that makes per-request state isolation possible, which is the foundation of the entire scoped variant. | |
| 9 | reasoning-9 | Reasoning | Explains that AsyncContext carries per-request data and is passed as a second argument to mergeObjectContext() so that events committed by the aggregate are tagged with the correct request context | critical | This helps developers understand why the scoped handlers thread context through mergeObjectContext(), rather than treating it as unnecessary boilerplate. | 8 |
| 10 | reasoning-10 | Reasoning | Explains that AsyncContext.merge(event, command) copies the async context from the incoming event onto the new command so the scoped handler receives the same request context that originated the event | critical | This helps developers understand why the scoped saga includes this extra step, and what breaks silently if it is omitted at the saga boundary. | 9 |
| 11 | reasoning-11 | Reasoning | Notes that the // logic comment in killEnemy() and addItem() is a placeholder for in-memory state mutation that the developer is expected to implement | bonus | This shows the model read the code carefully and understands that apply() records the event but does not mutate the aggregate's state on its own. | 3 |
| 12 | completeness-1 | Completeness | Answers how @CommandHandler and CommandBus together establish the mechanism by which dispatching a command calls the correct handler | critical | This is directly asked in the prompt. Leaving it out means the developer's first and most fundamental question goes unanswered. | |
| 13 | completeness-2 | Completeness | Answers what mergeObjectContext does to the hero instance | critical | This is directly asked in the prompt. The patching of publish() and publishAll() on the instance must be clearly identified. | |
| 14 | completeness-3 | Completeness | Answers why AggregateRoot.publish() alone is not enough to deliver events to the EventBus | critical | This is directly asked in the prompt. The reason mergeObjectContext is necessary must be stated explicitly, not implied. | |
| 15 | completeness-4 | Completeness | Answers how HeroesGameSagas receives events | critical | This is directly asked in the prompt. The source of events$ and CqrsModule's role in feeding it must be explained. | |
| 16 | completeness-5 | Completeness | Answers what happens to the DropAncientItemCommand returned from the map operator in the saga | critical | This is directly asked in the prompt. The automatic dispatch by CqrsModule must be identified, not left for the developer to guess. | |
| 17 | completeness-6 | Completeness | Answers where CqrsModule is registered and how its providers reach handlers inside HeroesGameModule | critical | This is directly asked in the prompt. The global module mechanism must be explained clearly, not just mentioned in passing. | |
| 18 | completeness-7 | Completeness | Answers what Scope.REQUEST does to the handler's lifecycle compared to the default singleton scope | critical | This is directly asked in the prompt. The contrast between the two scopes must be stated explicitly, not just one side described. | |
| 19 | completeness-8 | Completeness | Answers what AsyncContext is and why it is injected into the scoped handlers | critical | This is directly asked in the prompt. Naming AsyncContext without explaining what it carries and why it matters is not a complete answer. | |
| 20 | completeness-9 | Completeness | Answers what AsyncContext.merge(event, command) does in the scoped saga | critical | This is directly asked in the prompt. The context copying mechanism must be identified clearly. | |
| 21 | completeness-10 | Completeness | Answers why the regular HeroesGameSagas has no AsyncContext.merge call while the scoped saga does | critical | This is directly asked in the prompt. The contrast between the scoped and regular saga must be explained, not just one of them. | |
| 22 | style-1 | Style | Response contains no standalone code blocks, only inline references like apply(), AsyncContext, or mergeObjectContext() | critical | Code Onboarding responses are plain-language walkthroughs of existing code. Standalone code blocks suggest the model is rewriting rather than explaining. | |
| 23 | style-2 | Style | Response uses plain language aimed at a new team member and does not suggest changes or improvements to any of the provided files | critical | The prompt asks for explanation, not critique. Any suggestion of improvement signals the model misread the intent category. | |
| 24 | style-3 | Style | Traces the full runtime flow in sequence for both the regular and scoped variants: command dispatch, handler, aggregate, commit, EventBus, saga, new command, handler | bonus | This shows the model can synthesize the full picture across both variants rather than answering each question in isolation. | |
| 25 | penalty-1 | Penalty | States that apply() immediately dispatches the event to the EventBus at the moment it is called | penalty | This is factually wrong and would cause a developer to never call commit(), resulting in events that are silently dropped with no obvious error. | |
| 26 | penalty-2 | Penalty | States that publish() or publishAll() on AggregateRoot route events to the EventBus without mentioning that mergeObjectContext() must first patch those methods | penalty | This gives the developer a broken mental model where events appear to be wired up when they are not, making failures very hard to diagnose. | |
| 27 | penalty-3 | Penalty | States that CqrsModule must be explicitly imported in each feature module for its providers to be available | penalty | This is factually wrong and would lead the developer to add unnecessary imports across every feature module in the application. | |
| 28 | penalty-4 | Penalty | States that the saga manually dispatches DropAncientItemCommand by calling commandBus.execute() or commandBus.dispatch() inside the saga body | penalty | This is factually wrong and would cause the developer to write saga code that manually dispatches commands, which is not how the saga contract works. | |
| 29 | penalty-5 | Penalty | States that Scope.REQUEST creates a new handler instance per event rather than per incoming request | penalty | This is factually wrong and breaks any correct reasoning about handler isolation and how AsyncContext is scoped to a request. | |
| 30 | penalty-6 | Penalty | Suggests changes or improvements to any of the provided code files | penalty | The task is to explain existing code, not review it. Offering suggestions adds harmful content that misrepresents what the prompt is asking for. | |
