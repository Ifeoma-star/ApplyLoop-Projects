# Rubric — How Does the NestJS CQRS Heroes Module Actually Work?

## intent_category
Code Onboarding & Comprehension

## difficulty
Hard

---

## Rubric Evaluation

| ID | Section | Description | Weight | Rationale | Dependent On |
|---|---|---|---|---|---|
| reasoning-1 | Reasoning | States that @CommandHandler(KillDragonCommand) attaches metadata to the handler class that maps the command class to the handler | critical | This is the first half of the command routing mechanism. Without it, nothing explains how the framework knows which handler belongs to which command. | |
| reasoning-2 | Reasoning | States that CommandBus reads the metadata attached by @CommandHandler at registration time to build its internal handler map | critical | This is the second half of the routing mechanism. The mapping must be actively read and stored by CommandBus for dispatch to work at runtime. | reasoning-1 |
| reasoning-3 | Reasoning | Explains that mergeObjectContext() patches the publish() and publishAll() methods on the specific hero instance to route through the EventBus, replacing the no-op stubs that AggregateRoot provides by default | critical | Most developers assume AggregateRoot already routes events to the EventBus. Without identifying the patching step, the event delivery mechanism stays invisible. | reasoning-2 |
| reasoning-4 | Reasoning | Explains that apply() stages the event in AggregateRoot's internal event list without dispatching it | critical | If a model skips this, it cannot explain why events are not immediately visible to handlers the moment apply() is called. | reasoning-3 |
| reasoning-5 | Reasoning | Explains that commit() flushes the staged events by calling publishAll(), which routes each event through the EventBus | critical | Missing this means the model cannot explain what actually triggers event delivery or why calling commit() is required after every domain operation. | reasoning-4 |
| reasoning-6 | Reasoning | Explains that CqrsModule subscribes to the saga's returned Observable of ICommand at bootstrap and automatically dispatches every emitted command through the CommandBus | critical | A model that thinks the saga dispatches commands itself has misread the saga contract. This leads to structurally broken saga code. | reasoning-5 |
| reasoning-7 | Reasoning | Explains that @Saga() is discovered by CqrsModule at bootstrap, which calls dragonKilled with the global EventBus observable stream as events$ | critical | Without this, a developer cannot explain where events$ comes from or why the saga receives events without any manual subscription code. | |
| reasoning-8 | Reasoning | Explains that CqrsModule.forRoot() registers the module as globally scoped, making its exported providers available to all feature modules without each one importing CqrsModule | critical | Without knowing this, a developer will wrongly assume every feature module must import CqrsModule individually, which is a common NestJS mistake. | |
| reasoning-9 | Reasoning | Explains that Scope.REQUEST causes NestJS to create a new handler instance per incoming request rather than reusing a singleton | critical | This is the foundation of the entire scoped variant. Without it, none of the AsyncContext injection or context propagation makes sense. | |
| reasoning-10 | Reasoning | Explains that AsyncContext carries per-request data and is passed as a second argument to mergeObjectContext() so that events committed by the aggregate are tagged with the correct request context | critical | Without threading AsyncContext through mergeObjectContext(), committed events lose their request context and downstream scoped handlers break silently. | reasoning-9 |
| reasoning-11 | Reasoning | Explains that AsyncContext.merge(event, command) copies the async context from the incoming event onto the new command so the scoped handler receives the same request context that originated the event | critical | Without this call, the request context is lost at the saga boundary and the next scoped handler operates with a missing or mismatched context. | reasoning-10 |
| reasoning-12 | Reasoning | Notes that the // logic comment in killEnemy() and addItem() is a placeholder for in-memory state mutation that the developer is expected to implement | bonus | Catching this placeholder shows the model read the code carefully and understands what the aggregate is actually supposed to do beyond just following the call chain. | reasoning-4 |
| completeness-1 | Completeness | Answers what @CommandHandler does at the framework level | critical | Directly asked in the prompt. Skipping it leaves the developer's first question unanswered. | |
| completeness-2 | Completeness | Answers how CommandBus knows which handler to call for a given command | critical | Directly asked in the prompt. The routing mechanism must be explained, not just the decorator. | |
| completeness-3 | Completeness | Answers what mergeObjectContext does to the hero instance | critical | Directly asked in the prompt. The patching of publish() and publishAll() on the instance must be identified. | |
| completeness-4 | Completeness | Answers why AggregateRoot.publish() alone is not enough to deliver events to the EventBus | critical | Directly asked in the prompt. The reason mergeObjectContext is necessary must be stated separately from what it does. | |
| completeness-5 | Completeness | Answers how HeroesGameSagas receives events | critical | Directly asked in the prompt. The source of events$ and CqrsModule's role in feeding it must be explained. | |
| completeness-6 | Completeness | Answers what happens to the DropAncientItemCommand returned from the map operator in the saga | critical | Directly asked in the prompt. The automatic dispatch by CqrsModule must be identified. | |
| completeness-7 | Completeness | Answers where CqrsModule is registered and how its providers reach handlers inside HeroesGameModule | critical | Directly asked in the prompt. The global module mechanism must be explained, not just stated. | |
| completeness-8 | Completeness | Answers what Scope.REQUEST does to the handler's lifecycle compared to the default singleton scope | critical | Directly asked in the prompt. The contrast between the two scopes must be explicit. | |
| completeness-9 | Completeness | Answers what AsyncContext is and why it is injected into the scoped handlers | critical | Directly asked in the prompt. Naming AsyncContext without explaining what it carries is not a complete answer. | |
| completeness-10 | Completeness | Answers why AsyncContext is passed as a second argument to mergeObjectContext in the scoped handlers | critical | Directly asked in the prompt. The reason for threading context through mergeObjectContext must be addressed separately from what AsyncContext is. | |
| completeness-11 | Completeness | Answers what AsyncContext.merge(event, command) does in the scoped saga | critical | Directly asked in the prompt. The context copying mechanism must be identified. | |
| completeness-12 | Completeness | Answers why the regular HeroesGameSagas has no AsyncContext.merge call while the scoped saga does | critical | Directly asked in the prompt. The contrast between scoped and regular saga must be explained, not just one of them. | |
| style-1 | Style | Response contains no standalone code blocks, only inline references like apply(), AsyncContext, or mergeObjectContext() | critical | Code Onboarding responses are plain-language walkthroughs. Standalone code blocks signal the model is rewriting code rather than explaining it. | |
| style-2 | Style | Response uses plain language aimed at a new team member and does not suggest changes or improvements to any of the provided files | critical | The prompt asks for explanation, not critique. Any suggestions mean the model misread the intent category. | |
| style-3 | Style | Traces the full runtime flow in sequence for both the regular and scoped variants: command dispatch, handler, aggregate, commit, EventBus, saga, new command, handler | bonus | Covering both paths in sequence shows comprehension beyond just answering each question individually. | |
| penalty-1 | Penalty | States that apply() immediately dispatches the event to the EventBus at the moment it is called | penalty | Factually wrong. A developer who believes this will never call commit() and will spend hours debugging silently dropped events. | |
| penalty-2 | Penalty | States that publish() or publishAll() on AggregateRoot route events to the EventBus without mentioning that mergeObjectContext() must first patch those methods | penalty | Skipping the patching step gives the developer a broken mental model that makes silent failures nearly impossible to diagnose. | |
| penalty-3 | Penalty | States that CqrsModule must be explicitly imported in each feature module for its providers to be available | penalty | Directly wrong. This would cause the developer to pollute every feature module with unnecessary imports. | |
| penalty-4 | Penalty | States that the saga manually dispatches DropAncientItemCommand by calling commandBus.execute() or commandBus.dispatch() inside the saga body | penalty | Factually wrong. CqrsModule handles dispatch automatically. Stating otherwise produces non-functional saga code. | |
| penalty-5 | Penalty | States that Scope.REQUEST creates a new handler instance per event rather than per incoming request | penalty | Factually wrong. This confusion breaks any reasoning about handler isolation and how AsyncContext is scoped per request. | |
| penalty-6 | Penalty | Suggests changes or improvements to any of the provided code files | penalty | The task is to explain existing code, not review it. Suggestions are a net-new harmful addition that misrepresents the intent. | |
