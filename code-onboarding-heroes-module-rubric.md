# Rubric — How Does the NestJS CQRS Heroes Module Actually Work?

## intent_category
Code Onboarding & Comprehension

## difficulty
Hard

---

## Rubric Evaluation

| # | ID | Section | Description | Weight | Rationale | Dependent On |
|---|---|---|---|---|---|---|
| 1 | reasoning-1 | Reasoning | States that @CommandHandler registers the handler class as the designated processor for a specific command type using NestJS metadata, and that CommandBus reads this metadata at registration time to build its internal routing map | critical | The command-to-handler mapping is the very first thing a new developer needs to understand. Without it, the entire dispatch flow has no starting point. | |
| 2 | reasoning-2 | Reasoning | Explains that mergeObjectContext() patches the publish() and publishAll() methods on the specific hero instance to route through the EventBus, replacing the no-op stubs that AggregateRoot provides by default | critical | Many developers assume AggregateRoot handles EventBus routing out of the box. Identifying the patching step is what separates a correct mental model from a broken one. | 1 |
| 3 | reasoning-3 | Reasoning | Explains that apply() stages the event in AggregateRoot's internal event list without dispatching it | critical | A developer who skips this will be confused about why events never arrive at handlers even though apply() was called. Getting this right is central to understanding the event lifecycle. | 2 |
| 4 | reasoning-4 | Reasoning | Explains that commit() flushes the staged events by calling publishAll(), which routes each event through the EventBus | critical | Knowing what triggers actual event delivery is just as important as knowing how events are staged. Without commit(), no event ever reaches a handler or saga. | 3 |
| 5 | reasoning-5 | Reasoning | Explains that CqrsModule subscribes to the saga's returned Observable of ICommand at bootstrap and automatically dispatches every emitted command through the CommandBus | critical | Getting the saga contract wrong leads to structurally broken code. A developer must know that returning an Observable is the full extent of the saga's responsibility. | 4 |
| 6 | reasoning-6 | Reasoning | Explains that @Saga() is discovered by CqrsModule at bootstrap, which calls dragonKilled with the global EventBus observable stream as events$ | critical | Without knowing where events$ comes from, a developer has no way to explain how the saga receives events or why no manual subscription is needed. | |
| 7 | reasoning-7 | Reasoning | Explains that CqrsModule.forRoot() registers the module as globally scoped, making its exported providers available to all feature modules without each one importing CqrsModule | critical | Developers new to NestJS often assume each feature module needs its own import. Understanding global scoping clears up one of the most common points of confusion in this codebase. | |
| 8 | reasoning-8 | Reasoning | Explains that Scope.REQUEST causes NestJS to create a new handler instance per incoming request rather than reusing a singleton | critical | Everything about the scoped variant depends on understanding this lifecycle difference first. Miss it and the purpose of AsyncContext becomes completely unclear. | |
| 9 | reasoning-9 | Reasoning | Explains that AsyncContext carries per-request data and is passed as a second argument to mergeObjectContext() so that events committed by the aggregate are tagged with the correct request context | critical | Passing AsyncContext to mergeObjectContext() looks like boilerplate until you understand what it actually does. Explaining it prevents a developer from stripping it out thinking it is unnecessary. | 8 |
| 10 | reasoning-10 | Reasoning | Explains that AsyncContext.merge(event, command) copies the async context from the incoming event onto the new command so the scoped handler receives the same request context that originated the event | critical | Omitting this step at the saga boundary silently breaks request isolation in a way that is very hard to trace. Knowing why it exists prevents exactly that kind of bug. | 9 |
| 11 | reasoning-11 | Reasoning | Notes that the // logic comment in killEnemy() and addItem() is a placeholder for in-memory state mutation that the developer is expected to implement | bonus | Spotting this shows the model read beyond the call chain and understands that apply() records an event but does not mutate the aggregate's state on its own. | 3 |
| 12 | completeness-1 | Completeness | Answers how @CommandHandler and CommandBus together establish the mechanism by which dispatching a command calls the correct handler | critical | Directly asked in the prompt. Leaving it out means the developer's opening question goes unanswered and the rest of the explanation has no grounding. | |
| 13 | completeness-2 | Completeness | Answers what mergeObjectContext does to the hero instance | critical | Directly asked in the prompt. The patching of publish() and publishAll() must be named clearly or the event delivery mechanism stays unexplained. | |
| 14 | completeness-3 | Completeness | Answers why AggregateRoot.publish() alone is not enough to deliver events to the EventBus | critical | Directly asked in the prompt. Explaining what mergeObjectContext does is not enough on its own — the reason it is necessary must also be stated. | |
| 15 | completeness-4 | Completeness | Answers how HeroesGameSagas receives events | critical | Directly asked in the prompt. A response that skips the source of events$ leaves the developer wondering how the saga connects to anything. | |
| 16 | completeness-5 | Completeness | Answers what happens to the DropAncientItemCommand returned from the map operator in the saga | critical | Directly asked in the prompt. The automatic dispatch by CqrsModule is the key detail and must be identified explicitly. | |
| 17 | completeness-6 | Completeness | Answers where CqrsModule is registered and how its providers reach handlers inside HeroesGameModule | critical | Directly asked in the prompt. A vague answer about global scope without explaining how NestJS global modules work does not satisfy the question. | |
| 18 | completeness-7 | Completeness | Answers what Scope.REQUEST does to the handler's lifecycle compared to the default singleton scope | critical | Directly asked in the prompt. Describing only one scope without contrasting it with the other leaves the developer without the full picture. | |
| 19 | completeness-8 | Completeness | Answers what AsyncContext is and why it is injected into the scoped handlers | critical | Directly asked in the prompt. Naming the type without explaining what it carries and why it is needed is not a sufficient answer. | |
| 20 | completeness-9 | Completeness | Answers what AsyncContext.merge(event, command) does in the scoped saga | critical | Directly asked in the prompt. The context copying mechanism is specific and must be described clearly, not glossed over. | |
| 21 | completeness-10 | Completeness | Answers why the regular HeroesGameSagas has no AsyncContext.merge call while the scoped saga does | critical | Directly asked in the prompt. The contrast is the whole point of the question and both sides must be addressed. | |
| 22 | style-1 | Style | Response contains no standalone code blocks, only inline references like apply(), AsyncContext, or mergeObjectContext() | critical | Code Onboarding responses are walkthroughs in plain language. Standalone code blocks suggest the model is demonstrating rather than explaining existing code. | |
| 23 | style-2 | Style | Response uses plain language aimed at a new team member and does not suggest changes or improvements to any of the provided files | critical | The prompt is a comprehension task, not a code review. Any improvement suggestion means the model has misread what is being asked. | |
| 24 | style-3 | Style | Traces the full runtime flow in sequence for both the regular and scoped variants: command dispatch, handler, aggregate, commit, EventBus, saga, new command, handler | bonus | Connecting both paths into one coherent sequence shows the model truly understood the system, not just answered questions in isolation. | |
| 25 | penalty-1 | Penalty | States that apply() immediately dispatches the event to the EventBus at the moment it is called | penalty | Factually wrong. A developer who takes this at face value will never call commit() and will spend hours trying to figure out why their events never arrive. | |
| 26 | penalty-2 | Penalty | States that publish() or publishAll() on AggregateRoot route events to the EventBus without mentioning that mergeObjectContext() must first patch those methods | penalty | Leaving out the patching step creates a mental model where events appear to be wired when they are not, making the resulting failures extremely hard to diagnose. | |
| 27 | penalty-3 | Penalty | States that CqrsModule must be explicitly imported in each feature module for its providers to be available | penalty | Factually wrong and would send the developer down a path of adding unnecessary imports to every feature module across the whole application. | |
| 28 | penalty-4 | Penalty | States that the saga manually dispatches DropAncientItemCommand by calling commandBus.execute() or commandBus.dispatch() inside the saga body | penalty | Factually wrong. The saga only returns an Observable and CqrsModule handles the rest. Stating otherwise produces code that breaks the saga pattern entirely. | |
| 29 | penalty-5 | Penalty | States that Scope.REQUEST creates a new handler instance per event rather than per incoming request | penalty | Factually wrong. Mixing up per-event with per-request makes it impossible to reason correctly about handler isolation or AsyncContext scoping. | |
| 30 | penalty-6 | Penalty | Suggests changes or improvements to any of the provided code files | penalty | The prompt asks for explanation of existing code. Offering suggestions adds content that was never asked for and misrepresents the intent of the task. | |
