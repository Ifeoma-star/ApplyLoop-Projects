# Rubric — How Does the NestJS CQRS Heroes Module Actually Work?

## intent_category
Code Onboarding & Comprehension

## difficulty
Hard

---

## Rubric Evaluation

| ID | Section | Description | Weight | Rationale | Dependent On |
|---|---|---|---|---|---|
| reasoning-1 | Reasoning | States that `@CommandHandler(KillDragonCommand)` attaches metadata to the handler class that maps the command class to the handler, and that `CommandBus` reads this metadata at registration time to build its internal handler map | critical | This is the entry point of the entire flow. Without understanding how the command-to-handler mapping is established, nothing downstream — the handler execution, the aggregate methods, the event delivery — can be meaningfully explained. | |
| reasoning-2 | Reasoning | Explains that `mergeObjectContext()` patches the `publish()` and `publishAll()` methods on the specific hero instance to route through the `EventBus`, replacing the no-op stubs that `AggregateRoot` provides by default | critical | This is the most commonly misunderstood step in the CQRS pattern. Most developers assume `AggregateRoot` already routes events to the `EventBus` out of the box. Without identifying that `mergeObjectContext()` patches `publish()` on the specific instance, the event delivery mechanism remains invisible and unexplained. | reasoning-1 |
| reasoning-3 | Reasoning | Explains that `apply()` stages the event in `AggregateRoot`'s internal event list without dispatching it, and that `commit()` is what flushes those staged events by calling `publishAll()` | critical | The staging vs. flushing distinction is the core of the event lifecycle. Without knowing that `apply()` only queues the event and `commit()` is what actually delivers it, a developer will not understand why events sometimes appear to silently fail to arrive at handlers. | reasoning-2 |
| reasoning-4 | Reasoning | Explains that `CqrsModule` subscribes to the saga's returned `Observable<ICommand>` at bootstrap and automatically dispatches every emitted command through the `CommandBus` — the saga does not call `commandBus.execute()` manually | critical | A model that believes the saga manually calls dispatch has fundamentally misread the saga contract. The saga's role is solely to return an `Observable<ICommand>` — `CqrsModule` owns the dispatch step. This misconception would cause a developer to write structurally incorrect saga code. | reasoning-3 |
| reasoning-5 | Reasoning | Explains that `@Saga()` is discovered by `CqrsModule` at bootstrap, which calls `dragonKilled` with the global `EventBus` observable stream as `events$` and keeps the subscription alive for the lifetime of the application | critical | Without knowing that `CqrsModule` discovers `@Saga()` at bootstrap and feeds it the global `EventBus` stream, a developer cannot explain where `events$` comes from or why the saga is always listening. This is the framework-level wiring that makes sagas reactive without any manual subscription code. | |
| reasoning-6 | Reasoning | Explains that `CqrsModule.forRoot()` registers the module as globally scoped, making its exported providers — `CommandBus`, `EventBus`, `QueryBus`, `EventPublisher` — available to all feature modules without each feature module needing to import `CqrsModule` | critical | Without understanding `forRoot()` global registration, a developer will incorrectly assume each feature module must import `CqrsModule` individually. This is a common NestJS DI misconception that leads to unnecessary imports and confusion about why providers are available across module boundaries. | |
| reasoning-7 | Reasoning | Explains that `Scope.REQUEST` causes NestJS to create a new handler instance per incoming request rather than reusing a singleton, which is required when the handler must carry per-request state | critical | Without grasping that `Scope.REQUEST` creates a fresh handler instance per request, the purpose of injecting `AsyncContext` and threading it through `mergeObjectContext()` has no basis. This distinction is the foundation of the entire scoped variant and must be established before the context propagation chain can be explained. | |
| reasoning-8 | Reasoning | Explains that `AsyncContext` carries per-request contextual data and is passed as a second argument to `mergeObjectContext()` so that events committed by the aggregate are tagged with that request's context, enabling downstream scoped handlers to operate within the same request scope | critical | `AsyncContext` carries per-request data that must survive across async boundaries within a single request. Without threading it through `mergeObjectContext()`, events committed by the aggregate lose their request context and downstream scoped handlers cannot operate within the correct request scope. | reasoning-7 |
| reasoning-9 | Reasoning | Explains that `AsyncContext.merge(event, command)` copies the async context from the incoming event onto the new command so that when the `CommandBus` dispatches it, the scoped handler receives the same request context that originated the event | critical | Without `AsyncContext.merge`, the request context that existed on the saga's incoming event would not be carried over to the new command. When `CommandBus` dispatches the command to its scoped handler, that handler would receive a missing or mismatched request context, silently breaking per-request isolation. | reasoning-8 |
| reasoning-10 | Reasoning | Notes that the `// logic` comment in `killEnemy()` and `addItem()` is a placeholder for in-memory state mutation on the aggregate, and that the apply/commit pattern is identical whether the command was triggered by a direct user request or by a saga | bonus | The `// logic` placeholder signals that the aggregate's state mutation is intentionally left for the developer to implement. Recognising this distinction — that `apply()` records the event but does not mutate state on its own — shows a deeper reading of the code beyond just following the call chain. | reasoning-3 |
| completeness-1 | Completeness | Answers what `@CommandHandler` does at the framework level and how `CommandBus` knows which handler to invoke for a given command | critical | Directly asked in the prompt. A response that skips this leaves the developer's first and most fundamental question unanswered. | |
| completeness-2 | Completeness | Answers what `mergeObjectContext` does to the hero instance and why `AggregateRoot.publish()` alone is not sufficient to deliver events to the `EventBus` | critical | Directly asked in the prompt. This is the most technically nuanced question — both what `mergeObjectContext` does and why it is necessary must be addressed. Answering one without the other is incomplete. | |
| completeness-3 | Completeness | Answers how `HeroesGameSagas` receives events and what happens to the `DropAncientItemCommand` returned from the `map` operator | critical | Directly asked in the prompt. Both halves must be covered — how events arrive via the global `EventBus` stream, and how the returned command is automatically dispatched by `CqrsModule`. | |
| completeness-4 | Completeness | Answers where `CqrsModule` is registered and how its providers reach handlers and sagas inside `HeroesGameModule` without `CqrsModule` appearing in that module's `imports` array | critical | Directly asked in the prompt. A response that says "it uses global scope" without explaining the NestJS global module mechanism leaves the developer without an actionable mental model. | |
| completeness-5 | Completeness | Answers what `Scope.REQUEST` does to the handler's lifecycle compared to the default singleton scope | critical | Directly asked in the prompt. The contrast between request-scoped and singleton must be stated explicitly — describing one without the other leaves the difference unexplained. | |
| completeness-6 | Completeness | Answers what `AsyncContext` is and why it is injected and passed through `mergeObjectContext` in the scoped handlers | critical | Directly asked in the prompt. A response that only names `AsyncContext` without explaining what it carries and why passing it to `mergeObjectContext()` matters is incomplete. | |
| completeness-7 | Completeness | Answers what `AsyncContext.merge(event, command)` does in the scoped saga and why there is no equivalent call in the regular `HeroesGameSagas` | critical | Directly asked in the prompt. The contrast between the scoped and regular saga is the key insight — explaining only one side without the other does not satisfy the question as asked. | |
| style-1 | Style | Response contains no standalone code blocks — only inline code references such as `apply()`, `AsyncContext`, or `mergeObjectContext()` are acceptable | critical | Code Onboarding responses must be plain-language walkthroughs of existing code. Standalone code blocks indicate the model is rewriting or demonstrating rather than explaining, which is the wrong mode for this intent category. | |
| style-2 | Style | Response uses plain instructor-to-new-developer language and does not suggest changes or improvements to any of the provided code files | critical | The prompt is a comprehension task, not a code review. Any suggestions or improvements signal the model has misread the intent category entirely. | |
| style-3 | Style | Explanation traces the full end-to-end runtime flow in sequence: command dispatch → handler → aggregate → commit → EventBus → saga → new command → handler, for both the regular and scoped variants | bonus | Tracing the complete dual-path flow — regular and scoped — in a single coherent sequence demonstrates exceptional comprehension that goes beyond answering each question in isolation. | |
| penalty-1 | Penalty | States that `apply()` immediately publishes or dispatches the event to the `EventBus` at the moment it is called | penalty | Factually wrong. This propagates a fundamental misunderstanding of the staging/commit lifecycle and would cause a developer to write code that never calls `commit()`, resulting in events that are silently dropped. | |
| penalty-2 | Penalty | States that `publish()` or `publishAll()` on `AggregateRoot` route events to the `EventBus` without mentioning that `mergeObjectContext()` must first patch those methods on the instance | penalty | Omitting the patching step gives the developer a broken mental model. They will expect events to flow without ever calling `mergeObjectContext()`, leading to silent failures that are very difficult to debug. | |
| penalty-3 | Penalty | States that `CqrsModule` must be explicitly imported in each feature module for its providers to be available to that module's handlers | penalty | Directly contradicts how `CqrsModule.forRoot()` global scoping works. This would mislead the developer into cluttering every feature module with unnecessary imports. | |
| penalty-4 | Penalty | States that the saga manually dispatches `DropAncientItemCommand` by calling `commandBus.execute()` or `commandBus.dispatch()` inside the saga body | penalty | Factually wrong. The saga returns an `Observable<ICommand>` and `CqrsModule` handles dispatch automatically. Stating otherwise gives the developer an incorrect picture of the saga contract that would produce non-functional code. | |
| penalty-5 | Penalty | States that `Scope.REQUEST` creates a new handler instance per event rather than per incoming request | penalty | Factually wrong. Confusing per-event with per-request leads to incorrect assumptions about handler isolation, instance count, and how `AsyncContext` is scoped, making the scoped variant impossible to reason about correctly. | |
| penalty-6 | Penalty | Suggests changes or improvements to any of the provided code files | penalty | This is a net-new harmful addition that misrepresents the intent of the task. The prompt asks for explanation of existing code, not critique or refactoring suggestions. | |
